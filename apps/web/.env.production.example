# Production frontend environment (copy to apps/web/.env.production.local or inject at build)
NEXT_PUBLIC_API_BASE_URL=https://api.example.com/api/v1

# Optional observability (see P1.1B)
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_SENTRY_ENABLED=false
